"""Component modules extracted from main.py.

Sub-modules:

* ``cli``       — interactive CLI prompts (idea input, refinement, approval)
* ``document``  — pure-data PRD assembly from MongoDB documents
* ``resume``    — state restoration / resume from MongoDB
* ``startup``   — process management, output recovery, delivery orchestration
"""
