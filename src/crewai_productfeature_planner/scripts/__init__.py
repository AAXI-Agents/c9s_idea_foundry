"""Scripts sub-package — standalone utility modules.

Contains:
    - ``confluence_xhtml``  — Markdown → Confluence XHTML converter
    - ``logging_config``    — Centralised logging setup
    - ``ngrok_tunnel``      — ngrok tunnel management
    - ``preflight``         — Pre-flight environment checks
    - ``retry``             — Exponential backoff retry wrapper
"""
