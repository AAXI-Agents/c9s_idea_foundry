{
  "get": {
    "tags": [
      "Slack OAuth",
      "Slack OAuth"
    ],
    "summary": "Begin a tenant-scoped Slack install",
    "description": "Authenticated entrypoint for installing or reinstalling the Slack app. Issues a signed ``state`` token carrying the caller's enterprise/organization, then redirects the browser to Slack's OAuth authorize URL. The callback verifies the state and persists the resulting tokens scoped to that tenant.",
    "operationId": "slack_install_start_slack_oauth_install_get",
    "responses": {
      "200": {
        "description": "Redirect to slack.com/oauth/v2/authorize",
        "content": {
          "application/json": {
            "schema": {}
          }
        }
      }
    }
  }
}