{
  "get": {
    "tags": [
      "Ideas"
    ],
    "summary": "List ideas (paginated)",
    "description": "Return a paginated list of ideas, newest first.",
    "operationId": "list_ideas_ideas__get",
    "parameters": [
      {
        "name": "page",
        "in": "query",
        "required": false,
        "schema": {
          "type": "integer",
          "minimum": 1,
          "description": "Page number (1-based)",
          "default": 1,
          "title": "Page"
        },
        "description": "Page number (1-based)"
      },
      {
        "name": "page_size",
        "in": "query",
        "required": false,
        "schema": {
          "type": "integer",
          "maximum": 100,
          "minimum": 1,
          "description": "Items per page (1-100)",
          "default": 10,
          "title": "Page Size"
        },
        "description": "Items per page (1-100)"
      },
      {
        "name": "project_id",
        "in": "query",
        "required": false,
        "schema": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Filter by project ID",
          "title": "Project Id"
        },
        "description": "Filter by project ID"
      },
      {
        "name": "status",
        "in": "query",
        "required": false,
        "schema": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Filter by status (inprogress, completed, paused, failed, archived)",
          "title": "Status"
        },
        "description": "Filter by status (inprogress, completed, paused, failed, archived)"
      },
      {
        "name": "include_archived",
        "in": "query",
        "required": false,
        "schema": {
          "type": "boolean",
          "description": "Include archived ideas in the result. Defaults to False so that user-archived ('deleted') ideas do not reappear in the dashboard.",
          "default": false,
          "title": "Include Archived"
        },
        "description": "Include archived ideas in the result. Defaults to False so that user-archived ('deleted') ideas do not reappear in the dashboard."
      },
      {
        "name": "organization_id",
        "in": "query",
        "required": false,
        "schema": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Filter by organization (enterprise admins only)",
          "title": "Organization Id"
        },
        "description": "Filter by organization (enterprise admins only)"
      }
    ],
    "responses": {
      "200": {
        "description": "Successful Response",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/IdeaListResponse"
            }
          }
        }
      },
      "422": {
        "description": "Validation Error",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/HTTPValidationError"
            }
          }
        }
      }
    }
  }
}